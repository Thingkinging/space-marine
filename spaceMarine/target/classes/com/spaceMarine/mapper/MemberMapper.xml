<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spaceMarine.mapper.MemberMapper">
	<select id="getList" resultType="com.spaceMarine.vo.MemberVO">
		<![CDATA[
			select * from member_test
		]]>
	</select>

	<insert id="insert">
	<![CDATA[
		insert into member_test(co_cd, co_nm, biz_no ,co_no, co_gbn)
		values(#{co_cd}, #{co_nm},#{biz_no} , #{co_no}, #{co_gbn})
		]]>
	</insert>
	
	<select id="read" resultType="com.spaceMarine.vo.MemberVO">
		select co_cd, co_nm from member_test where co_cd = #{co_cd} and co_nm = #{co_nm}
	</select>

</mapper>