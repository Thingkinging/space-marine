<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spaceMarine.mapper.ProductMapper">
	<select id="getList" resultType="com.spaceMarine.vo.ProductVO">
		<![CDATA[
			select code, product, note, unit, price from Popup_Test
		]]>
	</select>
	
	<select id="read" resultType="com.spaceMarine.vo.ProductVO">
		<![CDATA[
			select code, product, note, unit, price from Popup_Test where price = #{price}
		]]>
	</select>
	
	<!-- 페이징 처리 -->
	<select id="getListWithPaging" resultType="com.spaceMarine.vo.ProductVO">
		<![CDATA[
			SELECT code, product, note, unit, price from popup_test orders LIMIT #{amount}
		]]>
	</select>
	
	<!-- 전체 데이터 개수 -->
	<select id="getTotalCount" resultType="int">
	<![CDATA[
		SELECT COUNT(*) FROM popup_test WHERE code > 0
		]]>
	</select>
	
</mapper>